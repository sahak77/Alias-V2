import type { Spacing } from '../types';

/** Spacing scale (pt) — identical across all themes, single source of truth. */
export const spacing: Spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};
