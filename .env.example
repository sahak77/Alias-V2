# Public, non-sensitive config only. Copy to `.env.local` and fill in.
# Variables prefixed with EXPO_PUBLIC_ are inlined into the JS bundle —
# never put secrets here. Use EAS Secrets for anything sensitive.
EXPO_PUBLIC_API_URL=https://api.example.com
