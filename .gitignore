# macOS
.DS_Store

# Dependencies / build output (each project also has its own .gitignore;
# these are workspace-level safety nets)
node_modules/
dist/
build/

# Env / secrets — never commit
.env
.env.*
!.env.example

# Logs
*.log
