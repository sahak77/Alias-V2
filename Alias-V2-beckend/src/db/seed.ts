/**
 * Seed script (`npm run db:seed`, executed by tsx). Stub until tables exist — will
 * seed `content_policy` + sample packs once the schema lands in src/db/schema.
 */
async function seed(): Promise<void> {
  console.log('[seed] No tables yet — nothing to seed. Add schema in src/db/schema, then implement.');
}

void seed();
